<Page
    x:Class="Lyric_Finder.LyricPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lyric_Finder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Height="1014">

    <Grid>
        <TextBlock HorizontalAlignment="Center" Margin="0,58,0,0" Text="{Binding Path=Title, Mode=OneWay}" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Center" Margin="0,95,0,0" Text="{Binding Path=Artist, Mode=OneWay}" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Center" Margin="20,194,20,20" Text="{Binding Path=Lyrics, Mode=OneWay}" TextWrapping="Wrap" VerticalAlignment="Top"/>
    </Grid>
</Page>
